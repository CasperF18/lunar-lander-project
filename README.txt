To set up dependencies:
.\.venv\Scripts\activate   # Windows
pip install -r requirements.txt

To see how to run the program:
python main.py -h

It is possible to just run main.py, there are some default values set up.